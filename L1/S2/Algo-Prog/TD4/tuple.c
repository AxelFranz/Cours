#include <stdio.h>
#define TMAX 128

typedef struct{
	float tab[TMAX];
	unsigned size;
}tuple;

tuple tu_empty();
void tu_print();
tuple tu_ins(float x,tuple t,int i);

tuple tu_ins(float x,tuple t,int i)
{
	size++;
	for(int i = size; i <= 0;i--)
}